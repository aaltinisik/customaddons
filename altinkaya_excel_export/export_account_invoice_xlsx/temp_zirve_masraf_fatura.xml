<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="account_invoice_zirve_masraf_fatura" model="xlsx.template">
        <field name="res_model">report.account.invoice</field>
        <field name="fname">zirve_masraf_fatura.xlsx</field>
        <field name="name">Zirve Masraf Fatura Şablonu</field>
        <field name="description">Zirve Muhasebe programına uygun biçimde faturaların içeri aktarılabilmesini sağlayan Excel şablonu.</field>
        <field name="input_instruction">
            {
                '__EXPORT__': {
                    'Fatura': {  # sheet can be name (string) or index (integer)
                        'results': {  # prefix with _CONT_ to continue rows from previous row field
                            'A2': '${object.date_invoice.strftime("%d.%m.%Y") or ""}#{border=thin_border;align=left;style=date}',
                            'B2': 'number${value or ""}#{border=thin_border;align=left;style=text}',
                            'C2': 'journal_id.name${value or ""}#{border=thin_border;align=left;style=text}',
                            'D2': 'partner_id.name${value or ""}#{border=thin_border;align=left;style=text}',
                            'E2': '${"\'%s" % (object.partner_id.vat or "")}#{border=thin_border;align=left;style=text}',
                            'F2': 'partner_id.vat${value or ""}#{border=thin_border;align=left;style=text}',
            'G2': '${"%.2f" % (object.currency_rate * sum(x.base for x in object.tax_line_ids if 0.0 &lt; x.base and 7 &lt; (x.amount/x.base*100) &lt; 9)) or 0.0}#{border=thin_border;align=left;style=text}',
            'H2': '${"%.2f" % (object.currency_rate * sum(x.amount for x in object.tax_line_ids if 0.0 &lt; x.base and 7 &lt; (x.amount/x.base*100) &lt; 9)) or 0.0}#{border=thin_border;align=left;style=text}',
            'I2': '${"%.2f" % (object.currency_rate * sum(x.base for x in object.tax_line_ids if 0.0 &lt; x.base and 17 &lt; (x.amount/x.base*100) &lt; 19)) or 0.0}#{border=thin_border;align=left;style=text}',
            'J2': '${"%.2f" % (object.currency_rate * sum(x.amount for x in object.tax_line_ids if 0.0 &lt; x.base and 17 &lt; (x.amount/x.base*100) &lt; 19)) or 0.0}#{border=thin_border;align=left;style=text}',

                            'K2': 'amount_untaxed${"%.2f" % (1.0/object.custom_rate * value) or ""}#{border=thin_border;align=left;style=text}',
                            'L2': 'amount_tax${"%.2f" % (1.0/object.custom_rate * value) or ""}#{border=thin_border;align=left;style=text}',
                            'M2': '${"%.f" % (object.amount_tax / object.amount_untaxed * 100) if object.amount_tax > 0.0 else 0}#{border=thin_border;align=left;style=text}',
                            'N2': '${"\'%s" % (object.partner_id.z_receivable_export or "")}#{border=thin_border;align=left;style=text}',
                            'O2': 'partner_id.z_receivable_export${value or ""}#{border=thin_border;align=left;style=text}',
                            'P2': 'currency_id.name${value or ""}#{border=thin_border;align=left;style=text}',
                        },
                    },
                },
            }
        </field>
    </record>

    <function model="xlsx.template" name="load_xlsx_template">
        <value eval="[ref('account_invoice_zirve_masraf_fatura')]"/>
    </function>
</odoo>